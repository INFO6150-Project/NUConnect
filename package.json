{
  "name": "nuconnect",
  "version": "1.0.0",
  "description": "An App to connect with NEU peers",
  "main": "NUConnect/index.html",
  "scripts": {
    "install-all": "cd ConnectRoom/client && npm install && cd ../server && npm install && cd ../../neu-connect-hub/client && npm install && cd ../server && npm install",
    "start": "npm run start --workspace ConnectRoom-client && npm run start --workspace ConnectRoom-server && npm run start --workspace neu-connect-hub-client && npm run start --workspace neu-connect-hub-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Group 2",
  "license": "ISC",
  "workspaces": [
    "ConnectRoom/client",
    "ConnectRoom/server",
    "neu-connect-hub/client",
    "neu-connect-hub/server"
  ]
}